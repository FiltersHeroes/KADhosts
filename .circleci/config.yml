version: 2
jobs:
  build:
    docker:
    - image: polishfiltersteam/vichs_image:2.26.6
    working_directory: ~/git/KADhosts
    steps:
    - checkout
    - run: |
        export VICHS_MAIN_PATH="$(pwd)"
        VICHS.sh ./KADhosts.txt ./KADhole.txt ./KADomains.txt
workflows:
  version: 2
  workflow:
    jobs:
      - build:
          context: org-global
